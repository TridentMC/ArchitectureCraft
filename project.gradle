ext {
    group = 'com.tridevmc'
    projectPackage = 'com.tridevmc.architecture'
    projectName = 'architecturecraft'

    useElytraVersionFormat = true
    version = '4'

    compoundVersion = '1.16.0-SNAPSHOT'
    compoundModules = ['all']

    minecraftVersion = "1.21.4"
    minecraftVersionRange = "[1.21.4,1.22)"

    mappingsChannel = "parchment"
    mappingsMinecraftVersion = "1.21.4"
    mappingsVersion = "2024.12.22"

    neoForgeVersion = "21.4.38-beta"
    neoForgeVersionRange = "[21.4,)"
    loaderVersionRange = "[2,)"
}

if (!ext.early) {
    repositories {
    }

    dependencies {
    }
}
